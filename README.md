# Description

This project demonstrates how to read a rotary encoder on `Arduino UNO Board` 
using an external interrupt.
Depending on the detected direction, the counter is incremented or decremented 
and the rotation direction and counter value are printed to the debug console.
